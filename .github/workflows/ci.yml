name: CI

on:
  workflow_dispatch:

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: Pipeline info
        run: echo "Use the Pipeline workflow for sequential checks and deployment gates."
